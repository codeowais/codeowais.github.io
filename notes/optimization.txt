#command for optimized thumb & thumb hover image [1000x1000]
ffmpeg -i input.png -vf "scale=1000:-1" -vcodec libwebp -lossless 0 -quality 75 -preset photo -map_metadata -1 thumb.webp

# command for optimized hero image [2560x1440]
ffmpeg -i input.png -vf "scale=2560:-1" -vcodec libwebp -lossless 0 -quality 75 -preset photo -map_metadata -1 hero.webp

# command for optimized hero-tablet image [1280x1433]
ffmpeg -i input.png -vf "scale=1280:-1" -vcodec libwebp -lossless 0 -quality 75 -preset photo -map_metadata -1 hero-tablet.webp

# command for optimized hero-mobile image [1200x1800]
ffmpeg -i input.png -vf "scale=1200:-1" -vcodec libwebp -lossless 0 -quality 62 -preset photo -map_metadata -1 hero-mobile.webp